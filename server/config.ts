export default {
  default: {
    enabled: true,
    provider: 'local',
    providerOptions: {
      sizeLimit: 1000000,
    },
    actionOptions: {},
  },
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  validator() {},
};
